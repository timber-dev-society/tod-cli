{
  "private": true,
  "author": "dious@pm.me",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.3.0",
    "turbo": "^1.1.2"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,md}\""
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
